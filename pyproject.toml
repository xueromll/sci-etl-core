[build-system]
requires = ["setuptools>=77", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sci-etl-core"
version = "0.4.1"
description = "Reusable, domain-agnostic ETL core for scientific text mining."
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]
requires-python = ">=3.10"
keywords = ["etl", "pipeline", "text-mining", "scientific-literature", "data-extraction"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Scientific/Engineering",
    "Topic :: Text Processing",
    "Typing :: Typed",
]

dependencies = [
    "pydantic>=2",
    "pyyaml",
    "python-dotenv",
    "beautifulsoup4",
    "lxml",
    "pandas",
]

[project.urls]
Homepage = "https://github.com/xueromll/sci-etl-core"
Documentation = "https://xueromll.github.io/sci-etl-core/"
Repository = "https://github.com/xueromll/sci-etl-core"
Issues = "https://github.com/xueromll/sci-etl-core/issues"

[project.optional-dependencies]
async = ["httpx", "aiofiles", "aiolimiter"]
llm = ["openai", "tiktoken"]
pdf = ["pdfplumber"]
sql = ["sqlalchemy>=2", "aiosqlite"]
viz = ["plotly", "aiofiles"]
cluster = ["scikit-learn", "numpy"]
embeddings = ["numpy", "openai"]
embeddings-local = ["numpy", "sentence-transformers"]
search = []
dev = ["pytest", "pytest-asyncio", "pytest-mock", "pytest-cov", "hypothesis"]
lint = ["ruff", "mypy", "types-PyYAML", "types-requests", "types-aiofiles"]
docs = [
    "mkdocs>=1.6,<2",
    "mkdocs-material>=9.7,<10",
    "mkdocstrings[python]>=1.0,<2",
    "mkdocs-click>=0.9,<1",
    "mike>=2.1,<3",
    "ruff",
]
full = [
    "httpx", "aiofiles", "aiolimiter",
    "openai", "tiktoken",
    "pdfplumber",
    "sqlalchemy>=2", "aiosqlite",
    "plotly",
    "scikit-learn", "numpy",
    "requests",
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
sci_etl_core = ["py.typed"]

[tool.pytest.ini_options]
markers = ["live: calls a real upstream source; deselected unless run with -m live"]
addopts = "-m 'not live'"

[tool.coverage.report]
fail_under = 100
show_missing = true
exclude_also = ["if TYPE_CHECKING:"]

[tool.ruff]
line-length = 120
target-version = "py310"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "ASYNC", "UP", "RUF", "PT"]
allowed-confusables = ["×"]

[tool.ruff.lint.per-file-ignores]
"src/sci_etl_core/**/__init__.py" = ["F401"]

[tool.mypy]
files = ["src/sci_etl_core"]
warn_unused_ignores = true
warn_redundant_casts = true

[[tool.mypy.overrides]]
module = [
    "sci_etl_core.embeddings.async_base",
    "sci_etl_core.exceptions",
    "sci_etl_core.exporters.async_base",
    "sci_etl_core.extractors.async_base",
    "sci_etl_core.llm.async_base",
    "sci_etl_core.models",
    "sci_etl_core.observability",
    "sci_etl_core.pipeline_async",
    "sci_etl_core.state.async_base",
]
disallow_untyped_defs = true
disallow_any_generics = true
warn_return_any = true
no_implicit_reexport = true

[[tool.mypy.overrides]]
module = [
    "aiolimiter.*",
    "lxml.*",
    "pandas.*",
    "pdfplumber.*",
    "plotly.*",
    "sentence_transformers.*",
    "sklearn.*",
    "sqlalchemy.*",
    "tiktoken.*",
]
ignore_missing_imports = true
